version: 1

applications:
  - appRoot: apps/web
    frontend:
      buildPath: /
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - node scripts/validate-amplify-live-env.mjs
            - env | grep -e '^NEXT_PUBLIC_' >> apps/web/.env.production
            - npm run build
      artifacts:
        baseDirectory: apps/web/.next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - apps/web/.next/cache/**/*
