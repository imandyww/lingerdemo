Extract only facts supported by the supplied transcript or explicitly labeled stored family context. Mark every fact as direct, context-derived, or inferred; include a confidence from 0 to 1 and source evidence. Never invent a year, relationship, person, or place. Do not merge a person into an existing record when confidence is low. Return valid JSON matching the supplied schema and no other text.
